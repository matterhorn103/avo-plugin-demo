# First some general metadata

# These fields are required:
name = "pyscript-demo"
version = "0.1.0"
description = "An example Python script plugin for Avogadro 2 to demonstrate the revamped plugin API in v2.0 onwards"
authors = [
    { name = "Amedeo Avogadro", email = "avogadro@unito.it" }
]
# These fields are optional:
readme = "README.md"

# Second, tell Avogadro what your plugin needs and what it can do

plugin-config = true  # Whether Avogadro should store configuration of your plugin

# List the features your plugin provides as entries under the appropriate feature type
# Each entry must specify an `identifier`, which must be the name of the script
# that provides the feature
# e.g. the first entry will be called using `python get-charges.py [ARGS]`
[[charges]]
identifier = "get-charges"
method-name = "Löwdiken analysis"

[[commands]]
identifier = "transmute"
path.menu = "Build"  # The menu within which the command's menu entry should appear
path.entry = { label = "Transmute into gold", priority = 300 }  # The text for the command's menu entry and its position within "Build"

[[commands]]
identifier = "movex"
path.menu = "Extensions"
path.submenu = { menu = "Demo", priority = 500 }  # Priority determines the position of "Demo" within "Extensions"
path.entry = { label = "Move X", priority = 220 }  # Priority determines the position of "Move X" within "Demo"

[[commands]]
identifier = "movey"
path.menu = "Extensions"
path.submenu = { menu = "Demo", priority = 500 }
path.entry = { label = "Move Y", priority = 210 }

[[commands]]
identifier = "move"
path.menu = "Extensions"
path.submenu = { menu = "Demo", priority = 500 }
path.entry = { label = "Move", priority = 200 }

[[commands]]
identifier = "hello"
path.menu = "Extensions"
path.submenu = { menu = "Demo", priority = 500 }
path.entry = { label = "Say Hello", priority = 100 }

[[energy]]
identifier = "aura"
method-name = "Molecular aura"

[[formats]]
identifier = "lor"
format-name = "Lorentzian output format"

[[generators]]
identifier = "qavocado"
program-name = "Quantum Avocado"
